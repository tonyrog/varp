// Simple formula that under saturate-1 should assign
// B = true
// C = false
// A = D
// A = -E

(A ->  B) && (~A  ->  B) &&
(A -> ~C) && (~A  ->  ~C) &&
(A ->  D) && (~A  ->  ~D) &&
(A -> ~E) && (~A  ->  E)
